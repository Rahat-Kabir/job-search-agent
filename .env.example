# Job Search Agent - Environment Variables

# Required: LLM
DEEPSEEK_API_KEY=your_deepseek_api_key

# Required: At least one search API
TAVILY_API_KEY=your_tavily_api_key
BRAVE_API_KEY=your_brave_api_key

# Optional: Enhanced scraping
FIRECRAWL_API_KEY=your_firecrawl_api_key

# Optional: Database (Phase 2)
DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5432/job_search

# Optional: Rate limiting (defaults to localhost)
REDIS_URL=redis://localhost:6379

# Optional: CORS allowed origins (defaults to http://localhost:5173)
CORS_ORIGINS=http://localhost:5173

# Optional: Observability
LANGSMITH_API_KEY=
LANGSMITH_TRACING=true
